export function load() {
  return {
    meta: {
      title: 'Signed in | Authenticator',
      description: 'Your Authenticator Cloud Sync sign-in was confirmed.',
      type: 'authenticator-sync-confirmed'
    }
  };
}
